# 基于python实现的批量删除文本
```
# 设置全局默认版本（推荐 3.11.9）
pyenv global 3.11.9

# 1. 创建虚拟环境
python -m venv venv

# 2. 激活环境 (Windows)
source venv/bin/activate

# 3. 安装 PyMuPDF
pip install PyMuPDF

# 执行代码
python ./src/main.py
```